{
  "name": "post-pilot",
  "version": "1.0.0",
  "private": true,
  "description": "Upload a creative and publish it directly to a Facebook Page.",
  "scripts": {
    "check": "find api lib -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "dependencies": {
    "googleapis": "^173.0.0",
    "pdfkit": "^0.15.0"
  },
  "engines": {
    "node": ">=20"
  }
}
